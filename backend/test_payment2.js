const { PrismaClient } = require('./node_modules/@prisma/client'); const prisma = new PrismaClient(); prisma.project.findFirst().then(p => { if(!p) return; const id = p.id; console.log('Project ID:', id); const http = require('http'); const req = http.request({ hostname: 'localhost', port: 3000, path: '/api/payments/create-order', method: 'POST', headers: { 'Content-Type': 'application/json' } }, (res) => { let data = ''; res.on('data', chunk => data += chunk); res.on('end', () => console.log('Response:', data)); }); req.write(JSON.stringify({ projectId: id, paymentType: 'ESCROW' })); req.end(); }).catch(console.error).finally(() => prisma.$disconnect());
